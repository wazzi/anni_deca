<?xml version="1.0" encoding="utf-8"?>

<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="vertical"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:weightSum="1">

    <TextView
        android:id="@+id/txtEventName"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:layout_marginLeft="20dp"
        android:layout_marginRight="20dp"
        android:textSize="@dimen/calendar_event_main_text"
        android:editable="true"
        android:text="This is some test text"/>
    <TextView
        android:id="@+id/txtEventDetails"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:layout_marginLeft="50sp"
        android:layout_marginRight="20dp"
        android:textSize="@dimen/calendar_event_details"
        android:text="This is some other test text but smaller than the prevous text"/>

</LinearLayout>